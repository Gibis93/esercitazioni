let firstNum;
let secondNum;

firstNum = parseInt (prompt ("numero 1"));
secondNum = parseInt (prompt ("numero 2"));

const operazione = () => alert (firstNum + secondNum);

setTimeout(operazione, 10000);
    
